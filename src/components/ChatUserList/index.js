import React from 'react'

const ChatUserList = ({chatUser}) => {

    const handleFunction = (userId) =>{

    }


    // let ChatUserJSX = chatUser.map((user =>{
    //     <div className='container'key={user.id} onClick={()=>handleFunction(user.id)}>
    //         <img src={user.picture}  />
    //         <div>{user.name}</div>
            

    //     </div>
    // })) 


  return (
    <div>ChatUserList</div>
  )
}

export default ChatUserList